# This file is a TEMPLATE — do not commit real credentials.
# Create the secret with:
#
#   kubectl create secret generic confluence-credentials \
#     --namespace hiviq \
#     --from-literal=CONFLUENCE_BASE_URL=https://moosoft.atlassian.net \
#     --from-literal=CONFLUENCE_EMAIL=your@email.com \
#     --from-literal=CONFLUENCE_API_TOKEN=your-api-token
#
apiVersion: v1
kind: Secret
metadata:
  name: confluence-credentials
  namespace: hiviq
type: Opaque
stringData:
  CONFLUENCE_BASE_URL: "https://moosoft.atlassian.net"
  CONFLUENCE_EMAIL: "REPLACE_ME"
  CONFLUENCE_API_TOKEN: "REPLACE_ME"
