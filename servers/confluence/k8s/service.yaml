apiVersion: v1
kind: Service
metadata:
  name: mcp-confluence
  namespace: hiviq
spec:
  type: NodePort
  selector:
    app: mcp-confluence
  ports:
    - name: mcp
      port: 3000
      targetPort: 3000
      nodePort: 30880
