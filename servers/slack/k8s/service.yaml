apiVersion: v1
kind: Service
metadata:
  name: mcp-slack
  namespace: hiviq
  labels:
    app: mcp-slack
spec:
  type: NodePort
  selector:
    app: mcp-slack
  ports:
    - name: mcp
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30881
