# This file is a TEMPLATE — do not commit real credentials.
# Create the secret with:
#
#   kubectl create secret generic jira-credentials \
#     --namespace hiviq \
#     --from-literal=JIRA_BASE_URL=https://yourcompany.atlassian.net \
#     --from-literal=JIRA_EMAIL=your@email.com \
#     --from-literal=JIRA_API_TOKEN=your-api-token
#
apiVersion: v1
kind: Secret
metadata:
  name: jira-credentials
  namespace: hiviq
type: Opaque
stringData:
  JIRA_BASE_URL: "https://yourcompany.atlassian.net"
  JIRA_EMAIL: "REPLACE_ME"
  JIRA_API_TOKEN: "REPLACE_ME"
