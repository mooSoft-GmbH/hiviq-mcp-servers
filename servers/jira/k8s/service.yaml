apiVersion: v1
kind: Service
metadata:
  name: mcp-jira
  namespace: hiviq
spec:
  type: NodePort
  selector:
    app: mcp-jira
  ports:
    - name: mcp
      port: 3000
      targetPort: 3000
      nodePort: 30882
